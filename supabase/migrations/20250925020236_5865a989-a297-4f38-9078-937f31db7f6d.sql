-- Simple comment change to trigger type regeneration
-- This will refresh the TypeScript types to include the new enum values
COMMENT ON TABLE public.notifications IS 'User notifications with updated enum types';